-#header部分
%header.user-container
  .user-header
    %h1.user-header__head
      = link_to root_path,class: "user-header__head__link" do
        = image_tag "fmarket_logo_red.svg", alt: "frima"
    %nav.user-header__nav
      %ol.user-header__nav__ol
        %li.active.user-header__nav__ol__progress
          会員情報
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          電話番号認証
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          お届け先住所入力
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          支払い方法
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          完了
          .user-header__nav__ol__progress__li

-# 基本的な個人情報入力フォーム
.steppage
  -# 入力フォーム全体
  .MemberInformationInput
    -# 入力フォームのタイトル
    .MemberInformationInput__title
      会員情報入力
      -# 入力フォーム中身書き換え
    .MemberInformationInput__content
      .MemberInformationInput__content--form
        %form{action: "", method: "post"}
        = form_for(@user, url: user_registration_path) do |f|
          = render "devise/shared/error_messages", resource: @user
          -# ニックネーム入力
          .field
            = f.label :ニックネーム
            %span.form_require
              必須
            %br/
            = f.text_field :nickname, placeholder: "例) メルカリ太郎", class: "input_default"
          -# メールアドレス
          .field
            = f.label :メールアドレス
            %span.form_require
              必須
            %br/
            = f.email_field :email, autofocus: true, autocomplete: "email",placeholder:"PC・携帯どちらでも可", class: "input_default"
                       
          -# パスワード入力
          .field
            = f.label :パスワード
            %span.form_require
              必須
            %br/
            = f.password_field :password, autofocus: true, autocomplete: "password", placeholder: "7文字以上の半角英数字", class: "input_default"
            %p.form_info
              ※ 英字と数字の両方を含めて設定してください
            %input#password-check{type: "checkbox"}
            %span
              パスワードを表示する
          .Identification
            本人確認
          .Identification--text
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          %br/

          %label
            お名前(全角) 
          %span.form_require
            必須
          %br/
          .field
            .form_flex
              = f.text_field :first_name, placeholder: "例) 山田", class: "input_default"
              = f.text_field :last_name, placeholder: "例) 彩", class: "input_default"
          %label
            お名前カナ(全角)
          %span.form_require
            必須
          %br/
          .field
            .form_flex
              = f.text_field :first_name_rattle, placeholder: "例) ヤマダ", class: "input_default"
              = f.text_field :last_name_rattle, placeholder: "例) アヤ", class: "input_default"
          %label
            生年月日 
          %span.form_require
            必須
          %br/
          != sprintf(f.date_select(:birthday, prefix:'birthday',with_css_classes:'XXXXX', prompt:"--",use_month_numbers:true, start_year:Time.now.year, end_year:1900, date_separator:'%s'),'年','月')+'日'

          %p.form_info
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .TermsOfService
            「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします
          .actions
            = f.submit "次へ進む",class: "btn_red"

  -# フッターを呼び込み
  = render "items/subfooter"