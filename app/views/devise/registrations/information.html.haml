-#header部分
%header.user-container
  .user-header
    %h1.user-header__head
      = link_to root_path,class: "user-header__head__link" do
        = image_tag "fmarket_logo_red.svg", alt: "frima"
    %nav.user-header__nav
      %ol.user-header__nav__ol
        %li.active.user-header__nav__ol__progress
          会員情報
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          電話番号認証
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          お届け先住所入力
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          支払い方法
          .user-header__nav__ol__progress__li
        %li.user-header__nav__ol__progress
          完了
          .user-header__nav__ol__progress__li
-# 基本的な個人情報入力フォーム
.steppage
  -# 入力フォーム全体
  .MemberInformationInput
    -# 入力フォームのタイトル
    .MemberInformationInput__title
      会員情報入力
      -# 入力フォーム中身書き換え
    .MemberInformationInput__content
      .MemberInformationInput__content--form  
      = form_for(@user, url: user_registration_path) do |f|
        = render "devise/shared/error_messages", resource: @user
        -# ニックネーム入力
        .field
          = f.label :nickname
          %br/
          = f.text_field :nickname
        -# 名前登録
        .field
          = f.label :first_name
          %br/
          = f.text_field :first_name
        -# 苗字登録
        .field
          = f.label :last_name
          %br/
          = f.text_field :last_name
        -# 名前の振り仮名
        .field
          = f.label :first_name_rattle
          %br/
          = f.text_field :first_name_rattle
        -# 苗字の振り仮名
        .field
          = f.label :last_name_rattle
          %br/
          = f.text_field :last_name_rattle
        -# 生年
        .field
          = f.label :birthyear
          %br/
          = f.number_field :birthyear
        -# 月
        .field
          = f.label :birthmonth
          %br/
          = f.number_field :birthmonth
        -# 日
        .field
          = f.label :birthday
          %br/
          = f.number_field :birthday
          -# メールアドレス
        .field
          = f.label :email
          %br/
          = f.email_field :email, autofocus: true, autocomplete: "email"
          -# パスワード
        .field
          = f.label :password
          - if @minimum_password_length
            %em
              (#{@minimum_password_length} characters minimum)
          %br/
          = f.password_field :password, autocomplete: "new-password"
        .field
          = f.label :password_confirmation
          %br/
          = f.password_field :password_confirmation, autocomplete: "new-password"
          -# ボタン
        .actions
          = f.submit "Next"
      = render "devise/shared/links"

      -# 入力フォーム中身
    .MemberInformationInput__content
      .MemberInformationInput__content--form
        %form{action: "", method: "post"}

          -# ニックネーム入力
          .form_group
            %label
              ニックネーム
            %span.form_require
              必須
            %br/
            %input.input_default{name: "nickname", type: "text", placeholder: "例) メルカリ太郎"}
            %br/

          -# メールアドレス入力
          .form_group
            %label
              メールアドレス
            %span.form_require
              必須
            %br/
            %input.input_default{name: "email", type: "email", placeholder:"PC・携帯どちらでも可"}
            %br/
            
          -# パスワード入力
          .form_group
            %label
              パスワード
            %span.form_require
              必須
            %input#password.field.input_default{type: "password", placeholder: "7文字以上の半角英数字"}
            %p.form_info
              ※ 英字と数字の両方を含めて設定してください
            %input#password-check{type: "checkbox"}
            %span
              パスワードを表示する

          .Identification
            本人確認
          .Identification--text
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          %br/
          %label
            お名前(全角) 
          %span.form_require
            必須
          %br/
          .form_flex
            %input.input_default{name: "first_name", type: "text", placeholder: "例) 山田", class: "half"}
            %input.input_default{:name => "last_name", :type => "text", :placeholder => "例) 彩", :class => "half"}
          %br/
          %label
            お名前カナ(全角)
          %span.form_require
            必須
          %br/
          .form_flex
            %input.input_default{name: "first_name_rattle", type: "text",placeholder: "例) ヤマダ"}
            %input.input_default{name: "last_name_rattle", type: "text",placeholder: "例) アヤ"}
          %label
            生年月日 
          %span.form_require
            必須

          -# 仮で作成、実装後コメントアウト外す
          -# != sprintf(f.date_select(:birthday, prefix:'birthday',with_css_classes:'XXXXX', prompt:"--",use_month_numbers:true, start_year:Time.now.year, end_year:1900, date_separator:'%s'),'年','月')+'日'
          %div
          %select#select_year{name: "birthyear"}
          年
          = succeed "\u{6708}" do
            %select#select_month{name: "birthmonth"}
          = succeed "\u{65e5}" do
            %select#select_day{name: "birthday"}
          %p.form_info
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .TermsOfService
            「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします

          -# 送信ボタン
          %button{type: "submit", class: "btn_red"}
            = link_to '次へ進む'
            
  -# フッターを呼び込み
  -# = render "items/subfooter"